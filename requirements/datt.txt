# =============================================================================
#  DATT environment  (Python 3.10) — ISOLATED legacy stack
#
#  DATT (https://github.com/KevinHuang8/DATT) pins a 2022-era stack that is
#  INCOMPATIBLE with the main env: old `gym` (not gymnasium), NumPy < 1.24,
#  PyTorch 1.13 + CUDA 11.7, stable-baselines3 1.x. That is why it gets its own
#  Python environment. Do not mix these packages with the main env.
#
#  Note: the PyTorch 1.13 cu117 wheel bundles its own CUDA 11.7 runtime, so it
#  runs fine on a machine whose driver supports CUDA 12 (e.g. the RTX 2080).
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/cu117

numpy==1.23.4
scipy==1.11.3
torch==1.13.0+cu117
gym==0.21.0
stable-baselines3==1.6.2
ale-py==0.7.4
imageio==2.9.0
joblib==0.14.1
meshcat==0.3.2
pillow
pygame==2.4.0
rich==13.6.0
tqdm==4.64.1
matplotlib          # let pip pick a 3.10-compatible build (the 3.2.1 pin is too old)
